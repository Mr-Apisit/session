import 'package:flutter/material.dart';

class BottmNavBarWidget extends StatelessWidget {
  const BottmNavBarWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}